spring:
  application:
    name: hotelDemo
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: hotelDemoApplication
            client-secret: 9nfmUroXDejMQnCSKxF3VNHukfKFPoaK
            scope: openid+profile+email
            authorization-grant-type: authorization_code
          google:
            client-id: <GOOGLE_CLIENT_ID>
            client-secret: <GOOGLE_CLIENT_SECRET>
        provider:
          keycloak:
            issuer-uri: ${app.security.jwt-issuer-uri}
  cache:
    type: redis
    redis:
      time-to-live: 10m
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  datasource:
    password: Password~1234
    url: jdbc:mysql://ajaydemosqlserver.mysql.database.azure.com:3306/hotel?sessionVariables=sql_mode='NO_ENGINE_SUBSTITUTION'&jdbcCompliantTruncation=false&DB_CLOSE_ON_EXIT=FALSE
    username: demouser
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

server:
  port: 8082
  
app:
  security:
    enabled: true
    jwt-issuer-uri: https://lemur-12.cloud-iam.com/auth/realms/hotel-demo

keycloak:
  realm: hotel-demo
  auth-server-url: https://lemur-12.cloud-iam.com/auth
  resource: hotelDemoApplication
  public-client: true
  bearer-only: true
  

  
