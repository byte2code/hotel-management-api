# Copy this file to docker-compose.override.yml to configure your local credentials
services:
  app:
    environment:
      - DB_HOST=localhost
      - DB_PASSWORD=changeme
      - KEYCLOAK_CLIENT_SECRET=changeme
